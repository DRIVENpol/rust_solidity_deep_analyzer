# Contract Interaction Report

**Shows how contracts interact with each other through function calls**

════════════════════════════════════════════════════════════════════════════════

════════════════════════════════════════════════════════════════════════════════
## 📋 Jackpot → ScaledEntropyProvider
════════════════════════════════════════════════════════════════════════════════

**2 total function calls**

### 🔹 `getEntropyCallbackFee()`

**Calls:**
  - 🔍 `getFee()` *[view]*

───────────────────────────────────────

### 🔹 `runJackpot()`

**Calls:**
  - 🔍 `getFee()` *[view]*


════════════════════════════════════════════════════════════════════════════════
## 📋 Jackpot → JackpotTicketNFT
════════════════════════════════════════════════════════════════════════════════

**6 total function calls**

### 🔹 `_validateAndStoreTickets()`

**Calls:**
  - ✏️ `mintTicket()` *[nonpayable]*

───────────────────────────────────────

### 🔹 `claimWinnings()`

**Calls:**
  - 🔍 `getTicketInfo()` *[view]*
  - ✏️ `burnTicket()` *[nonpayable]*

───────────────────────────────────────

### 🔹 `emergencyRefundTickets()`

**Calls:**
  - 🔍 `getTicketInfo()` *[view]*
  - ✏️ `burnTicket()` *[nonpayable]*

───────────────────────────────────────

### 🔹 `getTicketTierIds()`

**Calls:**
  - 🔍 `getTicketInfo()` *[view]*


════════════════════════════════════════════════════════════════════════════════
## 📋 Jackpot → JackpotLPManager
════════════════════════════════════════════════════════════════════════════════

**12 total function calls**

### 🔹 `_setNewDrawingState()`

**Calls:**
  - ✏️ `initializeDrawingLP()` *[nonpayable]*

───────────────────────────────────────

### 🔹 `emergencyWithdrawLP()`

**Calls:**
  - ✏️ `emergencyWithdrawLP()` *[nonpayable]*

───────────────────────────────────────

### 🔹 `finalizeWithdraw()`

**Calls:**
  - ✏️ `processFinalizeWithdraw()` *[nonpayable]*

───────────────────────────────────────

### 🔹 `initializeLPDeposits()`

**Calls:**
  - ✏️ `initializeLP()` *[nonpayable]*
  - ✏️ `setLPPoolCap()` *[nonpayable]*

───────────────────────────────────────

### 🔹 `initiateWithdraw()`

**Calls:**
  - ✏️ `processInitiateWithdraw()` *[nonpayable]*

───────────────────────────────────────

### 🔹 `lpDeposit()`

**Calls:**
  - ✏️ `processDeposit()` *[nonpayable]*

───────────────────────────────────────

### 🔹 `setGovernancePoolCap()`

**Calls:**
  - ✏️ `setLPPoolCap()` *[nonpayable]*

───────────────────────────────────────

### 🔹 `setLpEdgeTarget()`

**Calls:**
  - ✏️ `setLPPoolCap()` *[nonpayable]*

───────────────────────────────────────

### 🔹 `setNormalBallMax()`

**Calls:**
  - ✏️ `setLPPoolCap()` *[nonpayable]*

───────────────────────────────────────

### 🔹 `setReserveRatio()`

**Calls:**
  - ✏️ `setLPPoolCap()` *[nonpayable]*

───────────────────────────────────────

### 🔹 `setTicketPrice()`

**Calls:**
  - ✏️ `setLPPoolCap()` *[nonpayable]*


════════════════════════════════════════════════════════════════════════════════
## 📋 JackpotBridgeManager → Jackpot
════════════════════════════════════════════════════════════════════════════════

**4 total function calls**

### 🔹 `buyTickets()`

**Calls:**
  - ✏️ `ticketPrice()` *[unknown]*
  - ✏️ `currentDrawingId()` *[unknown]*
  - ✏️ `buyTickets()` *[nonpayable]*

───────────────────────────────────────

### 🔹 `claimWinnings()`

**Calls:**
  - ✏️ `claimWinnings()` *[nonpayable]*


════════════════════════════════════════════════════════════════════════════════
## 📋 JackpotLPManager → Jackpot
════════════════════════════════════════════════════════════════════════════════

**1 total function calls**

### 🔹 `getLPValueBreakdown()`

**Calls:**
  - ✏️ `currentDrawingId()` *[unknown]*



---

*Generated by MainnetReady - Solidity Enhanced Analyzer*
